<html>
    <head></head>
    <body style="margin: 0; padding: 0;">
        <iframe 
            id="sg-{{ content.slug }}"
            name = "sg-{{ content.slug }}"
            title="{{ content.title }}" 
            width="100%" 
            height="100%" 
            src="{{ content.link }}">
        </iframe>
    </body>
</html>